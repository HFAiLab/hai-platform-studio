import { describe, expect, it } from 'vitest'

describe('ailab-query-server > common-usage', () => {
  // 这里可以根据实际情况编写测试，需要按需补充 VITE_TOKEN 字段
  it('skip test', async () => {
    expect(true).toBe(true)
  })
})
