export const convertClientGroupFromDisplay = (display: string): string => {
  return display
}
export const convertClientGroupToDisplay = (client_group: string): string => {
  return client_group
}

export const HAI_FOOT_STATEMENT_CN = 'HAI 平台集群监控'
export const HAI_FOOT_STATEMENT_EN = 'hai-platform opensource'
