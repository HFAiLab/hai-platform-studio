/* eslint-disable @typescript-eslint/no-unused-vars */
export const getNodesPerfGrafanaURL = (props: {
  begin: string | number
  end: string | number
  nodeQueryStr: string
}) => {
  // 公开版本暂未提供 grafana 的功能
  return ``
}
